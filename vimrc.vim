
" PLUGING INSTALLATION
"""""""""""""""""""""""""""
" lightline configuration
call plug#begin('${HOME}/.zplugins/plugged')
Plug 'itchyny/lightline.vim'
call plug#end()

" Load the configuration associated to the plugin
source ${HOME}/.zplugins/lightline/config.vim

